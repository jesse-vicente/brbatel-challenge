import styled from 'styled-components';

export const Container = styled.button`
  width: 180px;
  height: 45px;
  border-radius: 25px;
  color: var(--shape);
  background: #d0b27e;
  border: 0;
  font-size: 1rem;
`;
